[project]
name = "static-encrypted-blog"
version = "0.1.0"
description = "A fully static encrypted blog generator"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=41.0.0",
    "jinja2>=3.0",
    "mistune>=3.0.0",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
